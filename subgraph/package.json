{
  "name": "ejara-tb-base",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ ejara-tb-base",
    "create-local": "graph create --node http://localhost:8020/ ejara-tb-base",
    "remove-local": "graph remove --node http://localhost:8020/ ejara-tb-base",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 ejara-tb-base",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.79.0",
    "@graphprotocol/graph-ts": "0.35.1"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
